name: student-performance-paper-research
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pandas
  - numpy
  - scikit-learn
  - imbalanced-learn
  - pytorch
  - psycopg2
  - python-dotenv
  - pyarrow
  - pip
  - pip:
      - pytest
      - optuna
      - xgboost
# Research-only extras for reproducing Phase 2/3/4 HPO and historical XGB tables.
# Active runtime (src/prediction) must not import xgboost or optuna.
