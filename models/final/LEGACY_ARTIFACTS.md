# Legacy model artifacts

`final_model_manifest.json` and several checkpoints in this directory predate
the evidence protocol. They are retained for traceability but are **not** the
final result source and must not be cited in the thesis.

Use `artifacts/final/<run_id>/run_manifest.json`, predictions and metrics for
the reproducible experiment evidence.
