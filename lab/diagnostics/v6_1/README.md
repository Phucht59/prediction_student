# V6.1 architecture diagnosis

V6.1 remains a non-authoritative architecture diagnosis. Its current source,
artifact, and report paths are retained outside `lab/` because V6.2 frozen
validation verifies those exact paths and checksums. This note marks the
logical laboratory location without breaking replay. A future relocation must
first introduce an immutable path-migration manifest and replay validation.
