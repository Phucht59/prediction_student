# C4-STAR archived

C4-STAR v2.1 is **not** the thesis Hybrid. It is a failed-gate experiment stored under `test_lab/`.

The Hybrid architecture under evaluation in this tree is **C0-R** (`experiments/hybrid_superiority_v2`). Serving Hybrid is unchanged.

Run scientific C0-R completion:

```bash
py -3.10 -u -m experiments.hybrid_superiority_v2.c0r_scientific
```
