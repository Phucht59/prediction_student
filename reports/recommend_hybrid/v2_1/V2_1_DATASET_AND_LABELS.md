# V2.1 dataset and labels

The candidate table is built from cutoff-safe OULAD history. Future behavior signals are normalized by opportunity and residualized against outer-training state models. Relevance is continuous (0.6 behavior residual plus 0.4 proximal residual when available); grade thresholds are training-only. No model-internal risk delta is a primary outcome.
