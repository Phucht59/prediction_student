# Recommendation tests

These are unit, integration and release regression tests for authority enforcement,
temporal routing, evidence constraints, LearningPlan serialization and final evaluation.
Run with `python -m pytest tests/recommend_hybrid -q`.
