# Recommendation source

This is the active evidence-policy runtime. Start with `pipeline.py`; shared contracts,
constraints and explanations are in `common/`, while dataset logic is in `uci/` and
`oulad/`. Do not change frozen prediction authority or policy constraints without a new
release protocol.
