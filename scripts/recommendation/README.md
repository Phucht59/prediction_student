# Recommendation scripts

Use `generate_plan.py` for a single plan, `build_final_recommendations.py` for release
artefacts, and `validate_final_evidence_recommender.py` for the final gate. Training
scripts are not a recommendation release authority.
