# Final recommendation artefacts

This directory contains the released evidence-policy registry, derived plan export,
technical metrics and checksums. It contains no neural-ranker checkpoint. Rebuild with
`scripts/recommend_hybrid/build_final_recommendations.py` and validate with
`scripts/recommend_hybrid/validate_final_evidence_recommender.py`.
