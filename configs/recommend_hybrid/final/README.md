# Final recommendation configuration

These files are the version-neutral configuration surface for the released recommendation
model on `main`:

- `recommendation.yaml` — frozen Recommendation protocol/configuration;
- `actions.yaml` — canonical five-action definitions;
- `feature_contract.yaml` — pre-cutoff feature/leakage contract;
- `literature_sources.yaml` — policy-design source registry;
- `llm_annotation_protocol.yaml` — external-review protocol.

Their scientific source is `Module_recomend@17b519b22e8b69c875d27547d097e6d3b76bc404`.
They are release aliases/copies only; they do not represent post-Panel-B retuning.
