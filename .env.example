# PostgreSQL settings for source/prediction lineage and governed recommendation persistence.
# DATABASE_URL takes precedence when set.
DATABASE_URL=

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=student_predict
POSTGRES_USER=student_predict_app
POSTGRES_PASSWORD=CHANGE_ME_AT_RUNTIME
