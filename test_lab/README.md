# test_lab — non-release research trees

Local / historical research that is **not** the current thesis model.

Current release authorities stay in the normal tree:

- Prediction: Phase 4 Hybrid C0 (`src/prediction`, `artifacts/prediction`, `reports/prediction/final`)
- Recommendation: Five-EBM-C0 V3 (`src/recommend_hybrid/v3`, `artifacts/recommend_hybrid/v3`, `reports/recommend_hybrid/v3`)
- Recommendation V2 frozen labels (still used by V3 portability): `artifacts/recommend_hybrid/final`

Everything under this folder was moved out of the release surface. Bytes are preserved. Do not treat these paths as current Hybrid C0 or Recommendation V3 authority.
