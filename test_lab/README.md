# test_lab

Archived research, extra tests, and experiment dumps. **Not** the thesis release.

Serving code is `src/prediction` (Hybrid CNN–BiLSTM) and `src/recommend_hybrid/v3` (Recommendation V).

`archived_from_release/` holds material moved off the main tree at lock time.
