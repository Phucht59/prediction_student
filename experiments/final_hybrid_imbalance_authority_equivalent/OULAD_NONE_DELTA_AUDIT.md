# OULAD NONE delta forensic audit

All scientific protocol fields, rows, labels, folds, seeds, threshold source, aggregation, and metric implementation match. The macro-F1 delta is below 0.001 and final label changes are threshold-boundary cases. The frozen execution record does not specify deterministic CUDA/cuDNN/TF32 controls, so the residual is classified as CUDA numerical nondeterminism.
